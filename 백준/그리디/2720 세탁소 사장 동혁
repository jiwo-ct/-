t = int(input())  # 테스트 케이스의 개수
coin = [25, 10, 5, 1]  # 쿼터, 다임, 니켈, 페니

# 테스트 케이스 만큼 입력을 받음
for i in range(t):
  data = int(input())  # 거스름돈 단위
  arr = [[] * 4 for _ in range(t)]

  for value in coin:
    temp = data // value  # 4 2 0 4
    arr[value].append(temp)  # 4 2 0 4
    data -= temp * value  # 0.24 0.04 0.04 0.00

  print(' '.join(map(str, arr)))
